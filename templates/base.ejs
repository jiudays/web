<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.metadata.title %> - <%= site.title %></title>
    <meta name="description" content="<%= page.metadata.description || site.description %>">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1><%= site.title %></h1>
        <nav>
            <a href="/">Home</a>
            <a href="/about/">About</a>
        </nav>
    </header>
    
    <main>
        <article>
            <h1><%= page.metadata.title %></h1>
            <% if (page.metadata.date) { %>
                <time datetime="<%= page.metadata.date %>">
                    <%= new Date(page.metadata.date).toLocaleDateString() %>
                </time>
            <% } %>
            
            <%- page.content %>
        </article>
    </main>
    
    <footer>
        <p>&copy; <%= currentYear %> <%= site.author %>. All rights reserved.</p>
    </footer>
</body>
</html>