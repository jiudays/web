<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= site.title %> - <%= site.description %></title>
    <meta name="description" content="<%= site.description %>">
    <meta name="author" content="<%= site.author %>">
    <meta name="keywords" content="<%= (site.keywords || []).join(',') %>">
    <link rel="stylesheet" href="/css/style.css">
    
    <% if (custom.googleAnalytics) { %>
    <script async src="https://www.googletagmanager.com/gtag/js?id=<%= custom.googleAnalytics %>"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', '<%= custom.googleAnalytics %>');
    </script>
    <% } %>
</head>
<body>
    <header class="header">
        <div class="container">
            <h1 class="site-title">
                <a href="/"><%= site.title %></a>
            </h1>
            
            <nav class="navigation">
                <% navigation.forEach(function(item) { %>
                    <a href="<%= item.url %>" class="nav-link"><%= item.title %></a>
                <% }) %>
            </nav>
        </div>
    </header>

    <main class="main">
        <div class="container">
            <section class="hero">
                <h1>欢迎来到 <%= site.title %></h1>
                <p class="hero-description"><%= site.description %></p>
            </section>

            <section class="posts-section">
                <h2>最新文章 (<%= totalPosts %> 篇)</h2>
                
                <% if (posts.length > 0) { %>
                    <div class="posts-grid">
                        <% posts.forEach(function(post) { %>
                            <article class="post-card">
                                <h3 class="post-title">
                                    <a href="<%= post.url %>"><%= post.title %></a>
                                </h3>
                                
                                <div class="post-meta">
                                    <time datetime="<%= post.date.toISOString() %>" class="post-date">
                                        📅 <%= post.formattedDate %>
                                    </time>
                                    
                                    <% if (post.tags && post.tags.length > 0) { %>
                                        <div class="post-tags">
                                            <% post.tags.forEach(function(tag) { %>
                                                <span class="tag">#<%= tag %></span>
                                            <% }) %>
                                        </div>
                                    <% } %>
                                </div>
                                
                                <% if (post.excerpt) { %>
                                    <p class="post-excerpt"><%= post.excerpt %></p>
                                <% } %>
                                
                                <a href="<%= post.url %>" class="read-more">阅读更多 →</a>
                            </article>
                        <% }) %>
                    </div>
                <% } else { %>
                    <div class="no-posts">
                        <p>暂无文章，敬请期待...</p>
                    </div>
                <% } %>
            </section>

            <section class="stats-section">
                <div class="stats-grid">
                    <div class="stat-item">
                        <h3><%= totalPosts %></h3>
                        <p>总文章数</p>
                    </div>
                    <div class="stat-item">
                        <h3><%= new Date().getFullYear() %></h3>
                        <p>当前年份</p>
                    </div>
                    <div class="stat-item">
                        <h3><%= posts.length > 0 ? new Date(posts[0].date).getFullYear() : new Date().getFullYear() %></h3>
                        <p>最新文章年份</p>
                    </div>
                </div>
            </section>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p><%= custom.copyright || '© ' + currentYear + ' ' + site.author + '. 保留所有权利.' %></p>
            
            <div class="social-links">
                <% social.forEach(function(link) { %>
                    <a href="<%= link.url %>" target="_blank" rel="noopener" class="social-link">
                        <%= link.name %>
                    </a>
                <% }) %>
            </div>
        </div>
    </footer>
</body>
</html>